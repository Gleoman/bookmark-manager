<h1> Bookmarks </h1>

<p> Welcome <%= current_user.email %> <p>


<ul id='links'>
  <% @bookmarks.each do |bm| %>
    <li>
      Title: <%= bm.title %>
      URL:   <%= bm.url   %>
      Tag:   <%= bm.tags.map(&:name).join(", ")  %>
    </li>
  <% end %>
</ul>
